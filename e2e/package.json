{
  "name": "@kvy/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Conformance harness (design §13 item 3): a scripted 20-step session against a real local stack (server + daemon + a simulated session process) exercising every wire primitive — permissions, mode switching, adoption, reconnect. Run pre-release as a release gate; also runs in CI as an ordinary vitest suite.",
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "exercise-flow": "tsx src/cli.ts"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.3.0",
    "@kvy/crypto": "workspace:*",
    "@kvy/wire": "workspace:*",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.5",
    "@kvy/server": "workspace:*",
    "@types/node": "^22.10.0",
    "kvy": "workspace:*",
    "tsx": "^4.23.12",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  }
}
