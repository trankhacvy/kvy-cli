# P0-0.1-root-claude-md — Write root CLAUDE.md

## What was done

Created `/CLAUDE.md` at the repo root per plan.md §16 line 618 (the last task of Phase 0
"0.1 Scaffold"). It documents:

- **Commands** — `pnpm install` (and the postinstall-builds-wire pattern), `pnpm build`,
  `pnpm typecheck`, `pnpm test`, `pnpm lint`/`lint:fix`, `--filter` scoping, and what
  `.github/workflows/ci.yml` runs (install → lint → build wire → typecheck → test).
- **Package layout** — `packages/wire` and `packages/crypto` (both exist today), plus
  the planned `packages/cli`, `packages/server`, `packages/web` (from plan.md §1's tree),
  marked `[planned]` since they don't exist yet.
- **Monorepo conventions** — pnpm workspaces (`packages/*` glob), strict TypeScript via
  shared `tsconfig.base.json`, the `@/*` → `./src/*` path alias per package, Biome as the
  single root-level formatter/linter, and the "wire builds first" rule.
- **Pointers** — `plan.md`, `falcon-system-design.md`, `falcon-prd.md`, and the existing
  `docs/protocol.md` / `docs/encryption.md` stubs.
- A closing note asking future phases to update this file as `cli`/`server`/`web` land.

## Sources consulted

- `plan.md` §1 (Monorepo scaffold), §16 (0.1 Scaffold checklist, task at line 618).
- Root `package.json`, `turbo.json`, `tsconfig.base.json`, `pnpm-workspace.yaml`,
  `biome.json`, `.github/workflows/ci.yml` — read directly to keep documented commands
  accurate rather than guessed.
- `packages/wire/package.json`, `packages/wire/tsconfig.json`, `packages/crypto/package.json`
  — confirmed the `@/` alias pattern and per-package build/typecheck/test scripts.

## Assumptions / scope decisions

- Kept the file minimal per the task's explicit instruction: commands + conventions only,
  no restatement of architecture, protocol, or product content (that lives in the linked
  docs).
- `cli`/`server`/`web` are listed as `[planned]` in the package table since their
  directories don't exist yet in this worktree (they're later Phase 0/1 tasks per
  plan.md §16 §0.4 onward) — the layout section is written so a future task can flip
  the label and add package-specific commands without restructuring the doc.
- No changes made to any other file; this task only adds `CLAUDE.md`.

## Verification

- `pnpm install` (worktree had no `node_modules`) then `pnpm build` — turbo reports
  `2 successful, 2 total` (full cache hit on `@falcon/wire` and `@falcon/crypto`), confirming
  the monorepo still builds cleanly with this addition.
