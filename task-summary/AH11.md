# AH11 — known-issues-cleanup

## What

Implements `docs/auth-ux-hardening-plan.md` item 11: `docs/known-issues.md` still carried two
resolved issues as if they were open, plus one already-landed follow-up wrongly implied to be
outstanding.

1. **Issue #4** ("Auth token lifecycle needs a re-architecture") — verified against current
   source that every gap it lists is closed:
   - Refresh tokens exist and rotate: `packages/cli/src/auth/tokenProvider.ts`.
   - The daemon re-authenticates: `packages/cli/src/daemon/machineClient.ts:465-474` (force-refresh
     on auth `connect_error`) and `:388-415` (proactive `renew-token` before expiry).
   - Device sessions + revocation exist: `device_sessions` table, `GET /v1/auth/sessions` +
     revoke routes wired into Settings → Devices (`DevicesSection.tsx`).
   - 15m access-token TTL with silent refresh (`require-auth.tsx`, `session.ts`).
   - WebSocket periodic re-validation — confirmed **already landed**, not a gap: see sub-task 3
     below.

   Removed the row (`known-issues.md:14`) and the whole section (former `<a id="issue-4">` block,
   `:100-139`) — no "DONE/Fixed" stub left in its place, per the file's own explicit
   no-growing-archive convention (`:2-6,27-29`): "When an issue is resolved and verified, remove
   its row from this table and its section below — don't mark it 'Fixed' and leave it here."
   The only genuinely-still-open follow-up issue #4 named (WS periodic re-validation) turned out
   to already be done too (see below), so nothing was carried forward into a new issue — it's
   already tracked as plan item 8 ("A session never reconciles server-side if the machine dies
   without a clean exit") for the one thing that's still genuinely open (machine
   offline-vs-needs-reauth status), which issue #4 also wasn't scoped to.

2. **Issue #14** (`/pair` approval page bounce loop) — confirmed AH1 (`pair-silent-refresh`,
   commit `1cb929c`) is merged into this branch's history
   (`git merge-base --is-ancestor 1cb929c HEAD` → true) and its `task-summary/AH1.md` documents
   full build/typecheck/test plus a real live-verification pass (registered an account, paired a
   real CLI via `falcon auth login`, hit the exact "signed-in-but-token-expired visitor to
   `/pair#eph`" scenario via a full navigation, landed on the confirm screen instead of being
   bounced to `/signin/`, clicked Approve, confirmed the CLI printed `Logged in to Falcon.` and
   the paired machine appeared on the web Sessions list). Issue #14 describes exactly that bug and
   names the same fix (`silentRefresh()` before the `isSignedIn()` gate, plus a retry in
   `approve()`) that AH1 shipped — same root cause, independently discovered. Removed the row
   (`known-issues.md:24`) and section (former `<a id="issue-14">` block, `:505-543`).

3. **WS periodic re-validation** — verified live in `packages/server/src/app/socket.ts:164-197`:
   `armExpiryTimer` schedules a hard disconnect at the *current* access token's own `exp`, and the
   `renew-token` socket handler re-verifies the token, re-checks `device_sessions.revokedAt`, and
   re-arms the timer on success (disconnecting otherwise). This is exactly the "decision on
   whether WebSocket connections should re-validate periodically" that issue #4's "What a real fix
   needs" section left open — it's done, so there was nothing left to carry forward from #4 into
   any other doc.

Also fixed a dangling link this deletion produced: `docs/issue-4-plan.md:4` had
`[known-issues.md #4](./known-issues.md#issue-4)`, an anchor link into the section just deleted.
Changed it to plain (non-linked) text referencing the same issue number, noting the
row/section were removed per `known-issues.md`'s own convention — the historical description of
what issue #4 covered is unchanged, only the now-dead anchor link is gone. This satisfies the
plan's own "What to verify": "no dangling `#issue-4` / `#issue-14` links elsewhere." (Checked
with `grep -rn "known-issues.md#issue-4\|known-issues.md#issue-14"` across `docs/` and all
`.ts`/`.tsx`/`.md` files — the only other hits are prose mentions of "known-issues.md #4" as an
inline citation in code comments (`tokenProvider.ts`, `machineClient.ts`) and one historical
checklist line in `issue-4-plan.md:667` recording a *different, earlier* worktree pass's finding
— none of those are markdown anchor links, so none are "dangling" in the broken-link sense; left
them as-is since rewriting historical narrative prose is out of this unit's scope.)

## Files changed

- `docs/known-issues.md` — removed issue #4's index row + section, issue #14's index row +
  section. Net -83 lines, no code touched.
- `docs/issue-4-plan.md` — replaced the now-dangling `known-issues.md#issue-4` anchor link with
  plain text (+3/-1 lines).

## Assumptions

- Sub-task 3 ("note the still-open item... is actually already landed... nothing left to carry
  forward from #4") was verification-only, not a doc edit: I confirmed `socket.ts:164-197`
  matches the plan's description and that issue #4's "what a real fix needs" WS bullet is fully
  addressed, so no new known-issues.md entry or edit to plan item 8 was warranted.
- Left the unrelated stale note at `docs/issue-4-plan.md:667` ("could not do: this worktree's
  known-issues.md has no issue #4 entry") untouched — it's a historical record from a prior,
  different pass/worktree state, not something this unit's sub-tasks asked me to reconcile, and
  rewriting it would misrepresent what that earlier pass actually observed.
- `pnpm lint` (biome via the root script) failed with `[warn] Linter process terminated abnormally
  (possibly out of memory)` on both the first run and the CLAUDE.md-documented automatic retry —
  but per AH1's task summary (same environment) this is the user's global `rtk` (Rust Token
  Killer) Claude Code hook intercepting the `biome`/`npx` invocation itself and failing before
  biome ever runs (confirmed: even a bare `npx biome --version` fails identically). Calling
  `node_modules/.bin/biome` directly bypasses it and works: `biome check .` found 25 pre-existing
  errors, all in unrelated `packages/cli/src/auth/*` files predating this change; biome doesn't
  lint Markdown at all, so this unit's actual diff (`docs/*.md`) has zero lint surface regardless.

## Verification

- `pnpm build` — 6/6 tasks succeeded (full turbo, including `@falcon/web`'s static export).
- `pnpm typecheck` — 11/11 tasks succeeded.
- `pnpm test` — 11/11 tasks succeeded: `falcon` 162 files/1950 tests, `@falcon/web` 155
  files/1200 tests, all green. (Two earlier ad-hoc `pnpm --filter falcon test` runs each hit one
  different flaky, timer-based test — `sessionRegistry.test.ts` once, `scanner.test.ts` once —
  both green on every other run including the final full `pnpm test`; pre-existing timing
  flakiness unrelated to this doc-only change, not a regression.)
- `node_modules/.bin/biome check .` (direct, bypassing the rtk-broken `pnpm lint`) — 25
  pre-existing errors, none in files this unit touched (biome doesn't process `.md` at all).
- Confirmed no dangling `known-issues.md#issue-4` / `#issue-14` anchor links remain anywhere in
  the repo (`grep -rn` across `docs/` and all `.ts`/`.tsx`/`.md`), aside from the one fixed above.
- Confirmed via `git merge-base --is-ancestor 1cb929c HEAD` that AH1 (the fix issue #14 describes)
  is an ancestor of this branch's `HEAD`, and read its `task-summary/AH1.md` for the record of its
  own build/test/live verification.

## Live verification

This unit is **doc-only** (two Markdown files, zero code/behavior change — deleting known-issue
records and fixing one internal cross-reference link). Per the task's own live-verification
rule ("required for anything that isn't a pure copy/doc-only change"), no tmux/Chrome-MCP E2E
pass was performed for this unit itself — there is no runtime behavior to exercise. The
underlying claims this cleanup depends on (issue #4's fixes, issue #14's fix via AH1) were
verified live by their own implementing units: AH1's `task-summary/AH1.md` (in this same
worktree ancestry) documents a real sign-in, real CLI pairing via `falcon auth login`, a real
Approve click, and confirmation the paired machine appeared on the web Sessions list — the exact
scenario issue #14 describes as broken, now confirmed fixed. I did not re-run that live pass
myself for AH11; I verified its ancestry (merged into this branch) and read its documented
evidence rather than fabricate a second live pass for a doc-only change.
