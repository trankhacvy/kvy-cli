# Reference-only codebases (not part of Kvy's own history)
happy/
superset/
omnara/
mobvibe/

# OS
.DS_Store

# Node / package managers
node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# scripts/build-binaries.sh output (bun-compiled standalone CLI binaries)
dist-binaries/

# Next.js
.next/
out/

# packages/web/scripts/build-worker.mjs output — API-URL-baked build artifact,
# regenerated by `pnpm build`/`pnpm dev`, same footing as .next/out above.
packages/web/public/crypto-worker.js

# Env
.env
.env.local

# Editor
.vscode/
.idea/

# Worktrees used by kvy-dev-loop
.worktrees/

# Agent tooling (skills, reports) — local environment artifacts, not project source
.agents/
.claude/
skills-lock.json
live-test-report.md

# Throwaway extraction dir used by packages/wire/scripts/check-additive-vs-base.ts
packages/wire/.wire-lint-*/

# Stale/superseded docs — kept on disk for reference, not tracked (2026-07-23 docs cleanup)
docs/.archive/
